# Optional. If set, the app uses this to generate a richer LLM-narrated
# compliance summary and more natural Q&A answers. If left unset, the app
# runs fully offline using its rule-based summarizer/QA engine.
OPENAI_API_KEY=
